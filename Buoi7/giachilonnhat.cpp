#include <bits/stdc++.h>
using namespace std;

int main() {
    long long a, b, c, d, e;
    cin >> a >> b >> c >> d >> e;

    long long da = max({a, b, c, d, e});

    cout << da << endl;

    return 0;
}
