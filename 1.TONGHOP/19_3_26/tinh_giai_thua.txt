#include <bits/stdc++.h>
using namespace std;


long long h(int n)
{
    if (n == 0) return 1;
    return (h(n - 1) * n)%123456789;
}


int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin>>n;
    cout<<h(n)<<"\n";
    return 0;
}