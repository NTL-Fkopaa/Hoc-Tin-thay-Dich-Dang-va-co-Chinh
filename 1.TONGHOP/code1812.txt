BAI 1:
#include <bits/stdc++.h>
using namespace std;
int n,r, a[100000];
int main() 
{
    cin>>n;
    for (int i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a, a+n);
    for (int i=0;i<n;i++){
        cout<<a[i]<<" ";
    }

}

BAI 2:
#include <bits/stdc++.h>
using namespace std;
int n,k, a[100000];
int main() 
{
    cin>>n>>k;
    for (int i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a, a+n);
    for (int i=0;i<n;i++){
    }
  cout<<a[k-1]<<endl;
}

BAI 3:
#include <bits/stdc++.h>
using namespace std;
int n,k,b=0,l=0,a[100000];
int main() 
{
    cin>>n;
    for (int i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a, a + n);
    
    for (int i=0;i<n/2;i++){
      b+=a[i];}
    for (int i=n/2;i<n;i++){
      l+=a[i];}
      
  cout<<abs(b-l)<<endl;
}

baif4:#include <bits/stdc++.h>
using namespace std;
int n,k,r,b=0,l=0,a[100000];
int main() 
{
    cin>>n;
    for (int i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a, a + n);
    
    l=abs(a[0] - a[1]) + abs(a[n-1] - a[n-2]);
    for(int i=1;i<n-1;i++){
      l += min(a[i] - a[i-1], a[i+1] - a[i]);
    }
    cout<<l<<endl;
}